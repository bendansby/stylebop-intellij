# Gradle daemon JVM args — IntelliJ Platform builds need a fair bit of heap.
org.gradle.jvmargs=-Xmx2048m -XX:MaxMetaspaceSize=512m
org.gradle.parallel=true
org.gradle.caching=true

# Kotlin code style
kotlin.code.style=official
